[project]
name = "flaskr"
version = "1.0.0"
description = "Blog Jardim de Flores - Tutorial Flask"
dependencies = [
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
